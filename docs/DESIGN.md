# Design

> **Status: stub.** This file is owned by the `impeccable` tooling and will hold the visual design system — color (OKLCH), typography, spacing, elevation, motion, component decisions. Generate the first cut with `$impeccable document` once `apps/web` is scaffolded with real components, then refine.
>
> Until then, the working token set is the `:root` block in [`.cursor/rules/ui-components.mdc`](../.cursor/rules/ui-components.mdc) (colors, spacing scale, type scale, radii) and the visual reference is the Claude Design prototype (see [`reference/design-prototype.md`](reference/design-prototype.md) — note that its visual style is exploratory, not load-bearing). The locked UI stack: Base UI (unstyled primitives) + CSS Modules + Phosphor icons + Inter / Fira Code, atomic design, Storybook. See [`.cursor/rules/ui-components.mdc`](../.cursor/rules/ui-components.mdc).
