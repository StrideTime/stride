# Stride API

Temporary Hono stub. The backend API shape is intentionally deferred while the frontend prototype is iterated with mocked data.

Run locally:

```sh
pnpm dev
```

The stub listens on `http://localhost:4000`.
```
